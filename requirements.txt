# Core web stack
flask>=3.0,<4
flask-restx>=1.3.0
werkzeug>=3.0,<4

# Utilities
python-dotenv>=1.0
requests>=2.31
psutil>=5.9
SQLAlchemy>=1.4
psycopg[binary]

# MySQL driver for SQLAlchemy (used when connecting to MySQL / RDS)
pymysql>=1.0

# Só mantenha estes se o seu código realmente usar:
jsonschema>=4.19
attrs>=23.1
pytz>=2023.3

# Obs.: não fixe dependências transitivas do requests (idna, urllib3, certifi, charset-normalizer)
# para evitar conflitos no build do Vercel.
